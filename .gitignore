node_modules/
.next/
dist/
.env
.env.local
*.log
.DS_Store
# scratch diagnostics (cannot be unlinked on the build mount; never commit them)
**/diag*.ts
# pnpm temp artifacts left by failed in-mount installs (mount forbids unlink)
_tmp_*
.pnpm-store/

# Personal / non-code working notes (not committed)
personal/

# Internal build/process docs + original source (kept on disk, not published)
BUILD_SPEC.md
PROGRESS.md
_source/
